{
  "client": "codex",
  "home_env": "CODEX_HOME",
  "default_home": "~/.codex",
  "config": {
    "format": "toml",
    "path": "config.toml",
    "managed_paths": [["mcp_servers", "memory-orchestrator"]],
    "patches": [
      {
        "path": ["features", "codex_hooks"],
        "value": true
      },
      {
        "path": ["mcp_servers", "memory-orchestrator"],
        "value": {
          "command": "uv",
          "args": ["run", "--no-sync", "--project", "{project_dir}", "mo-server", "serve-mcp", "--client", "codex"],
          "env": {"MO_CLIENT": "codex"}
        }
      }
    ]
  },
  "hooks": {
    "format": "json",
    "path": "hooks.json",
    "events": {
      "UserPromptSubmit": {
        "type": "command",
        "command": ["uv", "run", "--no-sync", "--project", "{project_dir}", "python", "{project_dir}/src/memory_orchestrator_mcp/hooks/user_prompt_submit.py", "--client", "codex"]
      },
      "Stop": {
        "type": "command",
        "command": ["uv", "run", "--no-sync", "--project", "{project_dir}", "python", "{project_dir}/src/memory_orchestrator_mcp/hooks/stop.py", "--client", "codex"]
      }
    }
  },
  "instructions": {
    "source": "src/memory_orchestrator_mcp/agents/memory-orchestrator.AGENTS.md",
    "destination": "AGENTS.md"
  }
}
